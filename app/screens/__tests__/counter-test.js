import React from 'react';
import renderer from 'react-test-renderer';

import Counter from '../counter';

it('renders a Counter using Snapshots', () => {
  // expect(renderer.create()).toMatchSnapshot();
});
